node_modules/
*.log
.DS_Store
CLAUDE.md
knowledge.md
# Working-process docs / scratch — never committed (see CLAUDE.md commit rule)
docs/
.superpowers/

# Eval harness — scenario specs + fixture (process artifact, never shipped)
eval/
.claude

# Per-file test timings — bin/test-run.js schedules longest-job-first from this
test/.timings.json
